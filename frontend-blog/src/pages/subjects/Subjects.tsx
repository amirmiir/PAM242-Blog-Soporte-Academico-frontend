import { FC } from 'react'
import NavBar from '../../components/nav-bar/NavBar'

const Subjects: FC = () => {
    return (
        <div>
            <NavBar />
        </div>
    )
}

export default Subjects